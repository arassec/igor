# Filesystem Connector
A file-connector providing access to the filesystem the igor server is running on.

# Parameters
There are no parameters to configure the connector. Access rights to files and directories are those of the user running the igor server.