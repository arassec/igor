- CopyFileAction -> "overwrite"-property to overwrite target files
- "Run job" only for saved jobs instead of supplied JSON
- Job state in DB ('running') and synchronized job-manager
- Keep filters for jobs and services in the store
- Unit-Tests (Apache Mina SSHD!)
- Warning when leaving job-editor and unsaved changes exist
- README.md
- ListFilesProvider -> SortCriteria (None, Timestamp, Filename)
- new: DeleteFileAction
- Job execution history in job-editor
- Job schedule under home
- Job status under home
- Home-View with last job runs and "next-to-run"-overview
- Persistence-service in DB instead of files
- Persistence-service editor
- Job dry-runs without cloning Tasks?
- Replace IgorData with JSONObject
- Configuration-View with system configurations
- Initial setup with configuration on first start
- Tags for jobs + grouping/filtering jobs by tag
- Export + Import of jobs and services (via JSON)
- Save Job-Service-References to warn about deleting a used service
- Authentication + Authorization (Frontend + Backend)
- I18N
- Job-Parameter: cancel on task fail (true|false)
- Job-Parameter: kill-switch with date to disable jobs
- Limit for number of IgorData in dry-run-results.

