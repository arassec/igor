# 'Missing Connector' Connector

## Description
Indicates a missing connector, that igor couldn't find during initialization! 
The connector cannot be used any more and all jobs referencing this connector will fail during execution.

A connector might be missing due to removed plugins, refactorings or otherwise deleted dependencies.
